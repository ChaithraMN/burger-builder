import React from 'react';

const toolbar = (props) => (
    <header>
        <div>Menu</div>
        <div>logo</div>
        <nav>
            ...
        </nav>
    </header>
);

export default toolbar;